---
phase: 06-article-page-restructure
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/app/[locale]/article/[slug]/page.tsx
autonomous: false
requirements: [ART-01, ART-02, ART-04, ART-08]

must_haves:
  truths:
    - "Article page.tsx is a slim composer under 200 lines"
    - "Article page opens with breadcrumbs above a full-width hero image"
    - "Reading time appears in the metadata area below the hero"
    - "Related articles use ArticleCard instead of TransferCard"
    - "All existing functionality preserved: structured data, sidebar, ads, tags, error handling"
  artifacts:
    - path: "src/app/[locale]/article/[slug]/page.tsx"
      provides: "Slim article page composer"
      min_lines: 100
      contains: "ArticleHero"
  key_links:
    - from: "src/app/[locale]/article/[slug]/page.tsx"
      to: "src/components/article/index.ts"
      via: "import of all 4 article components"
      pattern: "from.*@/components/article"
    - from: "src/app/[locale]/article/[slug]/page.tsx"
      to: "src/components/ArticleCard.tsx"
      via: "ArticleCard for related articles"
      pattern: "ArticleCard"
    - from: "src/app/[locale]/article/[slug]/page.tsx"
      to: "src/lib/reading-time.ts"
      via: "calculateReadingTime for content"
      pattern: "calculateReadingTime"
---

<objective>
Recompose article page.tsx as a slim composer (~150-200 lines) that imports and wires the 4 sub-components from Plan 01. Migrate related articles from TransferCard to ArticleCard. Remove all inline rendering that was extracted.

Purpose: Complete the decomposition by making page.tsx a clean orchestrator. The file should read like a table of contents: data fetching at top, composed components below.

Output: Rewritten page.tsx under 200 lines with all features preserved
</objective>

<execution_context>
@C:/Users/tarkm/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/tarkm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-article-page-restructure/06-CONTEXT.md
@.planning/phases/06-article-page-restructure/06-01-SUMMARY.md

@src/app/[locale]/article/[slug]/page.tsx
@src/components/article/index.ts
@src/components/article/ArticleHero.tsx
@src/components/article/ArticleMeta.tsx
@src/components/article/ArticleBreadcrumb.tsx
@src/components/article/ArticleBody.tsx
@src/lib/reading-time.ts
@src/components/ArticleCard.tsx
@src/components/Sidebar.tsx

<interfaces>
<!-- Components created by Plan 01 -->
From src/components/article/index.ts:
```typescript
export { ArticleBreadcrumb } from './ArticleBreadcrumb'
// Props: { locale: string; league?: string; articleTitle: string }

export { ArticleHero } from './ArticleHero'
// Props: { title: string; imageUrl?: string; league?: string }

export { ArticleMeta } from './ArticleMeta'
// Props: { publishedAt: string; updatedAt?: string; createdAt: string; readingTime: number; locale: string; league?: string; playerName?: string; fromClub?: string; toClub?: string; transferStatus?: string; transferFee?: string; dict: any }

export { ArticleBody } from './ArticleBody'
// Props: { content: string }
```

From src/lib/reading-time.ts:
```typescript
export function calculateReadingTime(content: string): number
```

From src/components/ArticleCard.tsx:
```typescript
export interface ArticleCardProps {
  variant?: "hero" | "standard" | "compact" | "mini"
  title: string
  href: string
  imageUrl?: string
  league?: string
  timeAgo?: string
  excerpt?: string
  priority?: boolean
  className?: string
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite page.tsx as slim composer with ArticleCard migration</name>
  <files>src/app/[locale]/article/[slug]/page.tsx</files>
  <read_first>
    - src/app/[locale]/article/[slug]/page.tsx (full file - current 716 lines)
    - src/components/article/index.ts (barrel exports from Plan 01)
    - src/components/article/ArticleHero.tsx (to understand exact props)
    - src/components/article/ArticleMeta.tsx (to understand exact props)
    - src/components/article/ArticleBreadcrumb.tsx (to understand exact props)
    - src/components/article/ArticleBody.tsx (to understand exact props)
    - src/lib/reading-time.ts (for calculateReadingTime import)
    - src/components/ArticleCard.tsx (for ArticleCardProps)
  </read_first>
  <action>
    Rewrite `src/app/[locale]/article/[slug]/page.tsx` to be a slim composer. Keep these sections EXACTLY as-is:

    **PRESERVE UNCHANGED (copy verbatim):**
    - `Article` interface (lines 39-57)
    - `getArticleBySlug` function (lines 65-179)
    - `getRelatedArticles` function (lines 182-196)
    - `generateMetadata` function (lines 199-354)
    - `generateStaticParams` function (lines 357-361)
    - `getTranslation` helper (lines 23-36)
    - JSON-LD structured data generation (lines 428-484)
    - Error handling try/catch wrapper (lines 487, 697-716)

    **REPLACE with component composition in the JSX return:**
    1. Remove `TransferCard` import, add: `import { ArticleBreadcrumb, ArticleHero, ArticleMeta, ArticleBody } from '@/components/article'`
    2. Add: `import { ArticleCard } from '@/components/ArticleCard'`
    3. Add: `import { calculateReadingTime } from '@/lib/reading-time'`
    4. Remove `ArrowLeft` import (no longer needed — breadcrumbs replace back button).
    5. Remove `Badge`, `Card`, `CardContent`, `Separator` imports if no longer used.
    6. Remove `Image` import (hero handles its own image).

    **In ArticlePage function, after data fetching:**
    - Calculate: `const readingTime = calculateReadingTime(article.content || '')`

    **JSX structure (the main return):**
    ```
    <main className="min-h-screen bg-background">
      {/* JSON-LD scripts — same as current */}

      {/* Breadcrumbs — above hero, inside container */}
      <div className="container mx-auto px-4 sm:px-6 lg:px-8 pt-4">
        <ArticleBreadcrumb locale={locale} league={article.league} articleTitle={article.title} />
      </div>

      {/* Hero Image — full-width */}
      <div className="container mx-auto px-4 sm:px-6 lg:px-8 mt-4">
        <ArticleHero title={article.title} imageUrl={article.image_url} league={article.league} />
      </div>

      {/* Content Grid */}
      <div className="container mx-auto px-4 sm:px-6 lg:px-8">
        <div className="grid grid-cols-1 lg:grid-cols-10 gap-4 md:gap-6 lg:gap-8 mt-6 md:mt-8">
          <article className="lg:col-span-7">
            {/* Meta: date + reading time */}
            <ArticleMeta
              publishedAt={article.published_at}
              updatedAt={article.updated_at}
              createdAt={article.created_at}
              readingTime={readingTime}
              locale={locale}
              league={article.league}
              playerName={article.player_name}
              fromClub={article.from_club}
              toClub={article.to_club}
              transferStatus={article.transfer_status}
              transferFee={article.transfer_fee}
              dict={dict}
            />

            {/* Article Body */}
            <ArticleBody content={article.content || ''} />

            {/* Tags — keep inline, small enough */}
            {article.tags && article.tags.length > 0 && (
              <div className="mt-12 pt-8 border-t border-border">
                ... tags rendering (same as current) ...
              </div>
            )}

            {/* After-article ad */}
            <div className="mt-8">
              <AdSlot placement="article.after-article" />
            </div>
          </article>

          {/* Sidebar */}
          <aside className="hidden lg:block lg:col-span-3" aria-label="Sidebar">
            <Suspense fallback={<SidebarSkeleton />}>
              <Sidebar locale={locale} dict={dict} />
            </Suspense>
          </aside>
        </div>

        {/* Related Articles — migrated from TransferCard to ArticleCard */}
        <section className="mt-12 md:mt-16">
          <h2 className="...">{getTranslation(dict, 'article.relatedArticles', 'Related Articles')}</h2>
          <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mt-4">
            {relatedArticles.slice(0, 4).map((transfer) => (
              <ArticleCard
                key={transfer.id}
                variant="standard"
                title={transfer.title}
                href={`/${locale}/article/${transfer.slug || transfer.id}`}
                imageUrl={transfer.imageUrl}
                league={transfer.league}
                timeAgo={formatTimeAgo(transfer.publishedAt, (key: string) => getTranslation(dict, key))}
                excerpt={transfer.excerpt}
              />
            ))}
          </div>
        </section>
      </div>
    </main>
    ```

    **Critical:** The rewritten file MUST be under 200 lines for the render function. Total file including data-fetching functions and generateMetadata can be longer (those are server functions), but the actual `ArticlePage` component function should be under 200 lines. Target total file around 400-450 lines (down from 716).

    **D-20 migration:** Replace all `TransferCard` references with `ArticleCard variant="standard"`. Map props: `primaryBadge` -> `league`, drop `imageAlt` (ArticleCard generates its own).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "ArticleHero" src/app/[locale]/article/[slug]/page.tsx` returns 0
    - `grep -q "ArticleBreadcrumb" src/app/[locale]/article/[slug]/page.tsx` returns 0
    - `grep -q "ArticleMeta" src/app/[locale]/article/[slug]/page.tsx` returns 0
    - `grep -q "ArticleBody" src/app/[locale]/article/[slug]/page.tsx` returns 0
    - `grep -q "calculateReadingTime" src/app/[locale]/article/[slug]/page.tsx` returns 0
    - `grep -q "ArticleCard" src/app/[locale]/article/[slug]/page.tsx` returns 0
    - `grep -cq "TransferCard" src/app/[locale]/article/[slug]/page.tsx` returns 1 (0 matches, grep exits non-zero)
    - `grep -q "from.*@/components/article" src/app/[locale]/article/[slug]/page.tsx` returns 0
    - File line count under 500: `wc -l` check
  </acceptance_criteria>
  <done>page.tsx is a slim composer importing all 4 article sub-components. TransferCard replaced by ArticleCard in related articles. Data fetching and metadata generation preserved. TypeScript compiles clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify article page visual quality</name>
  <what-built>
    Full article page restructure: hero image with gradient overlay, breadcrumb navigation, reading time display, decomposed components, and ArticleCard migration for related articles.
  </what-built>
  <how-to-verify>
    1. Start dev server: `npm run dev`
    2. Visit any article page (e.g., http://localhost:3000/en/article/[any-slug])
    3. Verify hero image:
       - Full-width image with dark gradient overlay at bottom
       - Article title displayed in white text over the gradient
       - League badge visible on the hero if article has a league
    4. Verify breadcrumbs:
       - Above the hero image: "Home > Premier League > Article Title..."
       - Home and league are clickable links
       - Article title is truncated if long
    5. Verify reading time:
       - Below the hero, in the metadata area
       - Shows clock icon + "X min read"
       - Date is also displayed
    6. Verify article body:
       - Content renders correctly with headings and paragraphs
       - Ad slots appear after paragraphs 3 and 6
       - Max line width is comfortable for reading (~65 characters)
    7. Verify related articles at bottom use card layout (not old TransferCard)
    8. Verify sidebar still appears on desktop
    9. Test on mobile (narrow browser): layout stacks properly, hero image scales
    10. Try an article without an image: should show bg-card fallback with title only
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual/functional issues</resume-signal>
</task>

</tasks>

<verification>
- `wc -l src/app/[locale]/article/[slug]/page.tsx` is under 500 lines total
- `grep -c "TransferCard" src/app/[locale]/article/[slug]/page.tsx` returns 0
- `grep "from.*@/components/article" src/app/[locale]/article/[slug]/page.tsx` shows the import
- `npx tsc --noEmit` passes
- `npm run build` succeeds (no build errors)
</verification>

<success_criteria>
- Article page.tsx is a slim composer using ArticleBreadcrumb, ArticleHero, ArticleMeta, ArticleBody
- TransferCard completely removed from article page, replaced by ArticleCard
- Hero image displays with gradient overlay and title text
- Breadcrumbs show navigable path above hero
- Reading time calculated and displayed with clock icon
- All existing functionality preserved (SEO, sidebar, ads, tags, error handling)
- Visual quality approved by user
</success_criteria>

<output>
After completion, create `.planning/phases/06-article-page-restructure/06-02-SUMMARY.md`
</output>
