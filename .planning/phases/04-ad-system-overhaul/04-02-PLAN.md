---
phase: 04-ad-system-overhaul
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/components/ads/index.ts
  - src/app/[locale]/page.tsx
  - src/app/[locale]/layout.tsx
  - src/app/[locale]/article/[slug]/page.tsx
  - src/components/TransferGrid.tsx
autonomous: true
requirements: [DS-07, AD-01, AD-02, AD-03]

must_haves:
  truths:
    - "No file in the codebase imports any of the 12 old ad components"
    - "All ad placements use AdSlot with a placement string"
    - "The ads barrel export only exports AdSlot and shouldShowAds"
    - "Old ad component files are deleted"
  artifacts:
    - path: "src/components/ads/index.ts"
      provides: "Clean barrel export with only AdSlot and shouldShowAds"
      exports: ["AdSlot", "shouldShowAds"]
    - path: "src/app/[locale]/page.tsx"
      provides: "Homepage using AdSlot for all ad placements"
      contains: "AdSlot"
    - path: "src/app/[locale]/article/[slug]/page.tsx"
      provides: "Article page using AdSlot for all ad placements"
      contains: "AdSlot"
  key_links:
    - from: "src/app/[locale]/page.tsx"
      to: "src/components/ads/AdSlot.tsx"
      via: "import from barrel"
      pattern: "AdSlot"
    - from: "src/components/TransferGrid.tsx"
      to: "src/components/ads/AdSlot.tsx"
      via: "import from barrel"
      pattern: "AdSlot"
---

<objective>
Migrate all ad consumers from 12 individual components to the unified AdSlot, update the barrel export, and delete old ad component files.

Purpose: Complete the consolidation by replacing all usage sites and removing dead code.
Output: All consumers use AdSlot, old files deleted, barrel export cleaned.
</objective>

<execution_context>
@C:/Users/tarkm/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/tarkm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ad-system-overhaul/04-CONTEXT.md
@.planning/phases/04-ad-system-overhaul/04-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — AdSlot component API -->
From src/components/ads/AdSlot.tsx:
```typescript
interface AdSlotProps {
  placement: string;   // "homepage.sidebar-top", "article.paragraph-1", etc.
  lazy?: boolean;      // override config default
  sticky?: boolean;    // mobile sticky behavior
  className?: string;
}
export function AdSlot({ placement, lazy, sticky, className }: AdSlotProps)
```

<!-- Placement string format: "{page}.{position}" where:
  page = homepage | article | league | search
  position = kebab-case of AD_SLOTS key (e.g. SIDEBAR_TOP -> sidebar-top)
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate all consumer files from old ad components to AdSlot</name>
  <files>src/app/[locale]/page.tsx, src/app/[locale]/layout.tsx, src/app/[locale]/article/[slug]/page.tsx, src/components/TransferGrid.tsx</files>
  <read_first>
    - src/components/ads/AdSlot.tsx
    - src/lib/ads.ts
    - src/app/[locale]/page.tsx
    - src/app/[locale]/layout.tsx
    - src/app/[locale]/article/[slug]/page.tsx
    - src/components/TransferGrid.tsx
  </read_first>
  <action>
**Migrate each consumer file (per D-15):**

**src/app/[locale]/page.tsx (homepage):**
1. Replace import: `import { AdInContent1, AdInContent2, AdInContent3, AdSidebar, AdSidebar2, AdMobileSticky } from '@/components/ads'` with `import { AdSlot } from '@/components/ads'`
2. Replace each component usage:
   - `<AdInContent1 />` -> `<AdSlot placement="homepage.post-hero" />`
   - `<AdInContent2 />` -> `<AdSlot placement="homepage.post-latest" />`
   - `<AdInContent3 />` -> `<AdSlot placement="homepage.pre-latest" />`
   - `<AdSidebar />` -> `<AdSlot placement="homepage.sidebar-top" />`
   - `<AdSidebar2 />` -> `<AdSlot placement="homepage.sidebar-middle-1" />`
   - `<AdMobileSticky />` -> `<AdSlot placement="homepage.mobile-sticky" sticky />`
3. Preserve the exact JSX position of each ad — just swap the component, keep surrounding layout.

**src/app/[locale]/layout.tsx:**
1. Replace import: `import { AdBanner } from '@/components/ads'` with `import { AdSlot } from '@/components/ads'`
2. Replace: `<AdBanner />` -> `<AdSlot placement="homepage.header" />`

**src/app/[locale]/article/[slug]/page.tsx:**
1. Replace import: `import { AdArticleContent, AdArticleBottom } from "@/components/ads"` with `import { AdSlot } from "@/components/ads"`
2. Replace each usage:
   - `<AdArticleContent key={...} />` at paragraph 3 -> `<AdSlot placement="article.paragraph-1" key={...} />`
   - `<AdArticleContent key={...} />` at paragraph 6 -> `<AdSlot placement="article.paragraph-3" key={...} />`
   - `<AdArticleBottom />` -> `<AdSlot placement="article.after-article" />`
3. Preserve the key props on in-content ads for React reconciliation.

**src/components/TransferGrid.tsx:**
1. Replace import: `import { AdInFeed } from '@/components/ads'` with `import { AdSlot } from '@/components/ads'`
2. Replace: `<AdInFeed />` -> `<AdSlot placement="homepage.in-latest-grid" />`
3. Keep the existing every-6-articles insertion logic unchanged.

**Important:** Each file should import ONLY `AdSlot` from `@/components/ads` — no other ad component imports. If `shouldShowAds` is needed in article page, import it too.
  </action>
  <verify>
    <automated>cd C:/Users/tarkm/Desktop/PROJECTS/transfersdaily/transfersdaily-app && npx tsc --noEmit src/app/[locale]/page.tsx src/app/[locale]/layout.tsx src/components/TransferGrid.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "AdInContent1\|AdInContent2\|AdInContent3\|AdSidebar2\|AdMobileSticky\|AdBanner\|AdInFeed\|AdArticleContent\|AdArticleBottom" src/app/ src/components/TransferGrid.tsx` returns NO matches
    - `grep "AdSlot" src/app/[locale]/page.tsx` returns at least 6 matches (6 ad placements)
    - `grep "AdSlot" src/app/[locale]/layout.tsx` returns at least 1 match
    - `grep "AdSlot" src/app/[locale]/article/[slug]/page.tsx` returns at least 3 matches
    - `grep "AdSlot" src/components/TransferGrid.tsx` returns at least 1 match
    - `grep 'placement="homepage.mobile-sticky"' src/app/[locale]/page.tsx` returns a match with `sticky` prop
  </acceptance_criteria>
  <done>All 4 consumer files import only AdSlot. No old ad component names appear in any consumer. All ad placements use placement strings.</done>
</task>

<task type="auto">
  <name>Task 2: Update barrel export and delete old ad component files</name>
  <files>src/components/ads/index.ts</files>
  <read_first>
    - src/components/ads/index.ts
    - src/components/ads/AdSlot.tsx
    - src/lib/content-validation.ts
  </read_first>
  <action>
**Update barrel export (per D-18):**

Replace entire contents of `src/components/ads/index.ts` with:
```typescript
export { AdSlot } from './AdSlot';
export { shouldShowAds } from '../../lib/content-validation';
```

This exports only `AdSlot` and `shouldShowAds` — no more individual ad component exports.

**Delete old ad component files (per D-17):**

Delete these 12 files (keep AdSense.tsx as internal primitive, keep AdSlot.tsx):
- src/components/ads/AdBanner.tsx
- src/components/ads/AdSidebar.tsx
- src/components/ads/AdSidebar2.tsx
- src/components/ads/AdInContent.tsx
- src/components/ads/AdInContent1.tsx
- src/components/ads/AdInContent2.tsx
- src/components/ads/AdInContent3.tsx
- src/components/ads/AdInFeed.tsx
- src/components/ads/AdMobileSticky.tsx
- src/components/ads/AdArticleContent.tsx
- src/components/ads/AdArticleBottom.tsx
- src/components/ads/AdFreeZone.tsx

Keep these files:
- src/components/ads/AdSense.tsx (internal primitive used by AdSlot)
- src/components/ads/AdSlot.tsx (the new unified component)
- src/components/ads/index.ts (updated barrel)

**Verify no remaining imports of deleted components:**
After deletion, run a grep across the entire src/ directory to confirm no file references any deleted component. If any orphan imports found, fix them.
  </action>
  <verify>
    <automated>cd C:/Users/tarkm/Desktop/PROJECTS/transfersdaily/transfersdaily-app && ls src/components/ads/ && echo "---" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/components/ads/` shows exactly 3 files: AdSense.tsx, AdSlot.tsx, index.ts
    - `cat src/components/ads/index.ts` contains exactly 2 export lines (AdSlot and shouldShowAds)
    - index.ts does NOT contain "AdBanner", "AdSidebar", "AdInContent", "AdInFeed", "AdMobileSticky", "AdArticleContent", "AdArticleBottom", "AdFreeZone"
    - `npx tsc --noEmit` produces no errors (no broken imports)
    - `grep -r "AdFreeZone\|AdBanner\|AdInContent1\|AdInContent2\|AdInContent3\|AdSidebar2\|AdInFeed\|AdArticleContent\|AdArticleBottom" src/` returns no matches
  </acceptance_criteria>
  <done>12 old ad component files deleted. Barrel export cleaned to only AdSlot and shouldShowAds. Full project type-checks with no broken imports.</done>
</task>

</tasks>

<verification>
1. `ls src/components/ads/` shows only AdSense.tsx, AdSlot.tsx, index.ts (3 files)
2. `grep -rn "from '@/components/ads'" src/` shows only AdSlot imports
3. `npx tsc --noEmit` passes with zero errors
4. `grep -r "AdInContent1\|AdInContent2\|AdInContent3\|AdSidebar2\|AdBanner\|AdInFeed\|AdMobileSticky\|AdArticleContent\|AdArticleBottom\|AdFreeZone" src/` returns nothing
5. Every ad placement in consumer files uses `<AdSlot placement="..." />` pattern
</verification>

<success_criteria>
- All 12 old ad component files are deleted
- Only 3 files remain in src/components/ads/: AdSense.tsx, AdSlot.tsx, index.ts
- All consumers use AdSlot with placement strings
- No file in src/ imports any old ad component
- Full project type-checks cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/04-ad-system-overhaul/04-02-SUMMARY.md`
</output>
