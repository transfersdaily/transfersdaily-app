---
phase: 05-homepage-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/[locale]/page.tsx
  - src/components/sections/HeroSection.tsx
  - src/components/sections/LatestSection.tsx
  - src/components/sections/LeagueSection.tsx
autonomous: true
requirements: [HOME-01, HOME-02, HOME-05, HOME-06]
must_haves:
  truths:
    - "Homepage renders the same visual output after decomposition (no regressions)"
    - "Each section component is under 150 lines"
    - "page.tsx is under 150 lines and acts as a slim composer"
    - "Homepage uses ISR with 5-minute revalidation instead of force-dynamic"
  artifacts:
    - path: "src/components/sections/HeroSection.tsx"
      provides: "Hero section with featured article + side articles"
      min_lines: 20
    - path: "src/components/sections/LatestSection.tsx"
      provides: "Latest transfers heading + TransferGrid"
      min_lines: 15
    - path: "src/components/sections/LeagueSection.tsx"
      provides: "Per-league article grid with compact cards"
      min_lines: 20
    - path: "src/app/[locale]/page.tsx"
      provides: "Slim page composer with ISR config"
      contains: "revalidate = 300"
  key_links:
    - from: "src/app/[locale]/page.tsx"
      to: "src/components/sections/HeroSection.tsx"
      via: "import and render with props"
      pattern: "import.*HeroSection"
    - from: "src/app/[locale]/page.tsx"
      to: "src/components/sections/LatestSection.tsx"
      via: "import and render with props"
      pattern: "import.*LatestSection"
    - from: "src/app/[locale]/page.tsx"
      to: "src/components/sections/LeagueSection.tsx"
      via: "import and render with props"
      pattern: "import.*LeagueSection"
---

<objective>
Decompose the 471-line homepage into clean section components and switch from force-dynamic to ISR.

Purpose: Make the homepage maintainable (small focused components) and faster (ISR caching instead of force-dynamic). HOME-01 (hero) is already done from Phase 3. HOME-02 (category feeds) is addressed by keeping existing league sections per user decision.

Output: 3 extracted section components in src/components/sections/, a slim page.tsx composer under 150 lines, and ISR with 5-minute revalidation.
</objective>

<execution_context>
@C:/Users/tarkm/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/tarkm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-homepage-redesign/05-CONTEXT.md

@src/app/[locale]/page.tsx
@src/components/ArticleCard.tsx
@src/components/TransferGrid.tsx
@src/components/Sidebar.tsx
@src/components/ViewAllButton.tsx
@src/components/ads/AdSlot.tsx
</context>

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/lib/api.ts:
```typescript
export type Transfer = {
  id: string;
  title: string;
  excerpt: string;
  league: string;
  status: string;
  publishedAt: string;
  imageUrl: string;
  slug: string;
  content?: string;
  transferValue?: string;
  playerName?: string;
  fromClub?: string;
  toClub?: string;
};
```

From src/lib/i18n.ts:
```typescript
export type Locale = 'en' | 'es' | 'it' | 'fr' | 'de';
export type Dictionary = Record<string, any>;
```

From src/components/ArticleCard.tsx:
```typescript
// CVA variants: hero, standard, compact, mini
// Props: variant, title, href, imageUrl, league?, timeAgo?, excerpt?, priority?
```

From src/components/TransferGrid.tsx:
```typescript
// Props: transfers, locale, dict, limit?
```

Current page.tsx structure:
- Lines 1-15: imports
- Lines 16-17: force-dynamic + revalidate=0
- Lines 19-115: generateMetadata (SEO)
- Lines 117-119: generateStaticParams
- Lines 122-168: LeagueSection local function
- Lines 170-338: HomePage component (renders hero, latest, sidebar, leagues)
- Lines 341-461: getInitialData server function
- Lines 464-471: generateSlug helper
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extract section components to src/components/sections/</name>
  <files>src/components/sections/HeroSection.tsx, src/components/sections/LatestSection.tsx, src/components/sections/LeagueSection.tsx</files>
  <read_first>src/app/[locale]/page.tsx, src/components/ArticleCard.tsx, src/components/TransferGrid.tsx, src/components/ViewAllButton.tsx</read_first>
  <action>
Create directory `src/components/sections/` and extract 3 components from page.tsx:

**HeroSection.tsx** — Extract lines 213-269 from page.tsx (the hero section JSX including Suspense fallback).
Props interface:
```typescript
interface HeroSectionProps {
  featuredTransfer: Transfer | null;
  latestTransfers: Transfer[];
  locale: Locale;
  t: (key: string, fallback?: string) => string;
}
```
- Keep the Suspense wrapper with ArticleCardSkeleton fallback
- Keep the grid layout: `grid-cols-1 md:grid-cols-3 gap-3 h-[420px]`
- Import ArticleCard, ArticleCardSkeleton from '@/components/ArticleCard'
- Import formatTimeAgo from '@/lib/date-utils'
- Import type Transfer from '@/lib/api', type Locale from '@/lib/i18n'
- Export as named export

**LatestSection.tsx** — Extract lines 272-299 (latest news grid with heading + TransferGrid).
Props interface:
```typescript
interface LatestSectionProps {
  transfers: Transfer[];
  locale: Locale;
  dict: Dictionary;
  t: (key: string, fallback?: string) => string;
}
```
- Keep the Suspense wrapper with skeleton fallback
- Slice transfers as `transfers.slice(0, 6)` (the slice(2,8) happens in page.tsx before passing)
- Import TransferGrid, ArticleCardSkeleton, ViewAllButton
- Export as named export

**LeagueSection.tsx** — Extract the existing LeagueSection function (lines 122-168).
Props interface (same as current, just moved to own file):
```typescript
interface LeagueSectionProps {
  title: string;
  slug: string;
  transfers: Transfer[];
  locale: Locale;
  dict: Dictionary;
  t: (key: string, fallback?: string) => string;
}
```
- Keep all existing JSX exactly as-is
- Import ArticleCard, ViewAllButton, formatTimeAgo
- Export as named export

Each component must be under 150 lines. Use `'use server'` is NOT needed — these are server components by default in Next.js app router.
  </action>
  <verify>
    <automated>ls -la src/components/sections/HeroSection.tsx src/components/sections/LatestSection.tsx src/components/sections/LeagueSection.tsx && wc -l src/components/sections/*.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export function HeroSection" src/components/sections/HeroSection.tsx
    - grep -q "export function LatestSection" src/components/sections/LatestSection.tsx
    - grep -q "export function LeagueSection" src/components/sections/LeagueSection.tsx
    - Each file under 150 lines (wc -l shows < 150 for each)
    - grep -q "ArticleCard" src/components/sections/HeroSection.tsx
    - grep -q "TransferGrid" src/components/sections/LatestSection.tsx
    - grep -q "variant=\"compact\"" src/components/sections/LeagueSection.tsx
  </acceptance_criteria>
  <done>Three section components exist in src/components/sections/, each under 150 lines, each exports a named function with correct props interface.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite page.tsx as slim composer with ISR</name>
  <files>src/app/[locale]/page.tsx</files>
  <read_first>src/app/[locale]/page.tsx, src/components/sections/HeroSection.tsx, src/components/sections/LatestSection.tsx, src/components/sections/LeagueSection.tsx</read_first>
  <action>
Rewrite page.tsx to be a slim composer that imports section components and delegates rendering. Target: under 150 lines for the page component + metadata, with getInitialData and generateSlug staying in the same file (they are server-only data fetching).

**ISR changes (HOME-06, decisions D-17 through D-20):**
- Remove `export const dynamic = 'force-dynamic'`
- Remove `export const revalidate = 0`
- Add `export const revalidate = 300` (5-minute ISR)
- Keep `generateStaticParams` for locale-based static generation
- Keep all data fetching server-side (no client-side fetching)

**Component changes:**
- Remove the inline LeagueSection function (now imported from sections/)
- Import HeroSection, LatestSection, LeagueSection from '@/components/sections/'
- Replace hero JSX block (lines 213-269) with `<HeroSection featuredTransfer={initialData.featuredTransfer} latestTransfers={initialData.latestTransfers} locale={locale} t={t} />`
- Replace latest section JSX (lines 272-299) with `<LatestSection transfers={initialData.latestTransfers.slice(2, 8)} locale={locale} dict={dict} t={t} />`
- Replace league section loop with: `{initialData.leagues.map((league) => (<LeagueSection key={league.id} title={league.name} slug={league.slug} transfers={initialData.leagueTransfers[league.slug] || []} locale={locale} dict={dict} t={t} />))}`

**Keep unchanged:**
- generateMetadata function (SEO metadata)
- generateStaticParams function
- getInitialData function (server-side data fetching, stays in page.tsx per decision D-16)
- generateSlug helper
- Sidebar + AdSlot placements
- JSON-LD structured data
- All responsive grid classes (lg:grid-cols-10, col-span-7, col-span-3)

The HomePage function body should be approximately 60-80 lines of clean JSX composition.
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "revalidate = 300" src/app/[locale]/page.tsx
    - grep -v "force-dynamic" src/app/[locale]/page.tsx (force-dynamic must NOT appear)
    - grep -v "revalidate = 0" src/app/[locale]/page.tsx (revalidate=0 must NOT appear)
    - grep -q "import.*HeroSection" src/app/[locale]/page.tsx
    - grep -q "import.*LatestSection" src/app/[locale]/page.tsx
    - grep -q "import.*LeagueSection" src/app/[locale]/page.tsx
    - grep -q "generateStaticParams" src/app/[locale]/page.tsx
    - grep -q "getInitialData" src/app/[locale]/page.tsx
    - The build succeeds (npx next build exits 0)
  </acceptance_criteria>
  <done>page.tsx is a slim composer under 150 lines (excluding getInitialData/metadata), imports 3 section components, uses ISR with revalidate=300, no force-dynamic, build succeeds with no errors.</done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- `wc -l src/components/sections/*.tsx` shows each file under 150 lines
- `grep -c "force-dynamic" src/app/[locale]/page.tsx` returns 0
- `grep "revalidate" src/app/[locale]/page.tsx` shows only `revalidate = 300`
- Homepage renders identically (same hero, latest, league sections, sidebar, ads)
</verification>

<success_criteria>
- Homepage page.tsx is a clean composer importing section components
- 3 section components extracted to src/components/sections/
- ISR with 5-minute revalidation replaces force-dynamic
- Build succeeds, no visual regressions
- HOME-01 addressed (hero already done in Phase 3)
- HOME-02 addressed (league sections kept per user decision)
- HOME-05 complete (decomposition done)
- HOME-06 complete (ISR switch done)
</success_criteria>

<output>
After completion, create `.planning/phases/05-homepage-redesign/05-01-SUMMARY.md`
</output>
