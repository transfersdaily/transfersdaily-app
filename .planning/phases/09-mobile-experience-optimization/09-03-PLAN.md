---
phase: 09-mobile-experience-optimization
plan: 03
type: execute
wave: 2
depends_on: ["09-01", "09-02"]
files_modified:
  - src/components/Pagination.tsx
  - src/components/ViewAllButton.tsx
  - src/app/globals.css
  - src/components/Footer.tsx
autonomous: true
requirements: [MOB-01, MOB-05]

must_haves:
  truths:
    - "All interactive elements meet 48px minimum tap target size on mobile"
    - "Adjacent touch targets have at least 8px gap"
    - "Pagination buttons are 48px tall on mobile"
    - "ViewAllButton has adequate touch target padding"
    - "No horizontal scrolling on any public page from 320px to 1440px+"
    - "touch-target utility class uses 48px (not 44px)"
  artifacts:
    - path: "src/components/Pagination.tsx"
      provides: "Touch-friendly pagination with 48px targets"
      contains: "min-h-[48px]"
    - path: "src/components/ViewAllButton.tsx"
      provides: "Touch-friendly view all button"
      contains: "min-h-[48px]"
    - path: "src/app/globals.css"
      provides: "Updated touch-target utility at 48px"
      contains: "min-h-[48px]"
    - path: "src/components/Footer.tsx"
      provides: "Footer links with adequate touch targets"
      contains: "min-h-[48px]"
  key_links:
    - from: "src/app/globals.css"
      to: "touch-target class"
      via: "utility class definition"
      pattern: "touch-target"
---

<objective>
Audit and fix touch targets across all public-facing interactive elements, and ensure responsive layouts work from 320px to 1440px+ without horizontal scrolling.

Purpose: The 65-75% mobile audience gets reliably tappable UI elements that meet Material Design's 48px minimum, and no layout breaks on any screen width.
Output: Updated components with 48px touch targets, updated touch-target utility, verified responsive layouts.
</objective>

<execution_context>
@C:/Users/tarkm/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/tarkm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-mobile-experience-optimization/09-CONTEXT.md

@src/components/Pagination.tsx
@src/components/ViewAllButton.tsx
@src/components/Footer.tsx
@src/app/globals.css
@src/components/navbar.tsx

<interfaces>
From src/app/globals.css (current touch-target utility):
```css
@layer utilities {
  .touch-target {
    @apply min-h-[44px] min-w-[44px] flex items-center justify-center;
  }
}
```
Note: Currently 44px, needs to be 48px per MOB-01.

From src/components/Pagination.tsx:
```typescript
// Uses Button with size="sm" — these are too small for mobile touch
// Page number buttons: className="w-10" (40px wide, no min-height)
// Prev/Next buttons: size="sm" with no explicit height
```

From src/components/ViewAllButton.tsx:
```typescript
// Just a Link with text classes, no padding for touch target
// className="font-display text-xs font-semibold uppercase tracking-wide text-primary..."
```

From navbar.tsx:
```typescript
// NavLink: already has min-h-[44px] — needs bump to 48px
// Dropdown triggers: already have min-h-[44px] — needs bump to 48px
// Icon buttons: already have min-h-[44px] min-w-[44px] — needs bump to 48px
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix touch targets on Pagination, ViewAllButton, and Footer</name>
  <files>src/components/Pagination.tsx, src/components/ViewAllButton.tsx, src/components/Footer.tsx</files>
  <read_first>src/components/Pagination.tsx, src/components/ViewAllButton.tsx, src/components/Footer.tsx</read_first>
  <action>
1. Update `src/components/Pagination.tsx`:
   - Previous/Next buttons: change `size="sm"` to `size="default"` and add `min-h-[48px]` class
   - Page number buttons: change `size="sm"` to `size="default"`, change `className="w-10"` to `className="min-w-[48px] min-h-[48px]"`
   - Gap between buttons: the existing `gap-2` (8px) meets the D-03 requirement
   - Add `gap-2` between prev button, page numbers, and next button (already has `gap-2` on flex container)

2. Update `src/components/ViewAllButton.tsx`:
   - Add touch-friendly padding: `min-h-[48px] px-3 py-2 inline-flex items-center`
   - Keep existing text styling (font-display, text-xs, uppercase, etc.)
   - The link should be easy to tap on mobile

3. Update `src/components/Footer.tsx`:
   - Read the file first to understand its structure
   - Ensure all footer links have `min-h-[48px]` and adequate padding for mobile tapping
   - Add `py-2` or `min-h-[48px] inline-flex items-center` to footer link elements
   - Ensure gap between adjacent footer links is at least 8px (per D-03)
  </action>
  <verify>
    <automated>cd C:/Users/tarkm/Desktop/PROJECTS/transfersdaily/transfersdaily-app && grep -c "min-h-\[48px\]" src/components/Pagination.tsx; grep -c "min-h-\[48px\]" src/components/ViewAllButton.tsx; grep -c "min-h-\[48px\]" src/components/Footer.tsx; grep "min-w-\[48px\]" src/components/Pagination.tsx | head -3</automated>
  </verify>
  <acceptance_criteria>
    - grep "min-h-\[48px\]" src/components/Pagination.tsx returns at least 2 matches (prev/next + page buttons)
    - grep "min-h-\[48px\]" src/components/ViewAllButton.tsx returns at least 1 match
    - grep "min-h-\[48px\]" src/components/Footer.tsx returns at least 1 match
    - grep "min-w-\[48px\]" src/components/Pagination.tsx returns at least 1 match (page number buttons)
  </acceptance_criteria>
  <done>Pagination, ViewAllButton, and Footer all have 48px minimum touch targets with adequate spacing</done>
</task>

<task type="auto">
  <name>Task 2: Update touch-target utility and bump navbar targets from 44px to 48px</name>
  <files>src/app/globals.css, src/components/navbar.tsx</files>
  <read_first>src/app/globals.css, src/components/navbar.tsx</read_first>
  <action>
1. Update `src/app/globals.css`:
   - Change `.touch-target` utility from `min-h-[44px] min-w-[44px]` to `min-h-[48px] min-w-[48px]`
   - This is the global utility that any component can use

2. Update `src/components/navbar.tsx` — bump all 44px references to 48px:
   - NavLink: change `min-h-[44px]` to `min-h-[48px]`
   - Leagues dropdown trigger: change `min-h-[44px]` to `min-h-[48px]`
   - Transfers dropdown trigger: change `min-h-[44px]` to `min-h-[48px]`
   - Dropdown menu items: change `min-h-[44px]` to `min-h-[48px]`
   - Search button: change `min-h-[44px] min-w-[44px]` to `min-h-[48px] min-w-[48px]`
   - User menu button: change `min-h-[44px] min-w-[44px]` to `min-h-[48px] min-w-[48px]`
   - Login button: change `min-h-[44px] min-w-[44px]` to `min-h-[48px] min-w-[48px]`
   - Mobile menu trigger: change `min-h-[44px] min-w-[44px]` to `min-h-[48px] min-w-[48px]`
   - Mobile sheet links: change `min-h-[44px]` to `min-h-[48px]`
   - Mobile search button: change `min-h-[44px]` to `min-h-[48px]`

   This is a straightforward find-and-replace of `44px` to `48px` in the navbar file.

3. Verify no horizontal overflow:
   - Check that `.mobile-container` has `overflow-x-hidden` or container widths don't exceed viewport
   - The existing `container` class with `px-4 md:px-6 lg:px-8` should prevent overflow
   - Add `overflow-x-hidden` to body in globals.css if not present, as a safety net: `body { overflow-x: hidden; }` (under the existing body rule in @layer base)
  </action>
  <verify>
    <automated>cd C:/Users/tarkm/Desktop/PROJECTS/transfersdaily/transfersdaily-app && grep "min-h-\[48px\]" src/app/globals.css; grep -c "44px" src/components/navbar.tsx; grep -c "48px" src/components/navbar.tsx; grep "overflow-x" src/app/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - grep "min-h-\[48px\]" src/app/globals.css returns match in touch-target utility
    - grep -c "44px" src/components/navbar.tsx returns 0 (all bumped to 48px)
    - grep -c "48px" src/components/navbar.tsx returns at least 8 matches
    - grep "overflow-x" src/app/globals.css returns match for hidden on body
  </acceptance_criteria>
  <done>Touch-target utility uses 48px, all navbar interactive elements use 48px, body has overflow-x-hidden safety net</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes without errors
- Zero instances of `min-h-[44px]` remain in navbar (all bumped to 48px)
- touch-target utility in globals.css uses 48px
- Pagination buttons are 48px tall
- ViewAllButton has 48px touch target
- Footer links have 48px touch target
- Body has overflow-x-hidden
</verification>

<success_criteria>
- All interactive elements across public pages meet 48px minimum tap target
- Adjacent touch targets have at least 8px gap
- No horizontal scrolling on any viewport from 320px to 1440px+
- touch-target CSS utility updated to 48px standard
</success_criteria>

<output>
After completion, create `.planning/phases/09-mobile-experience-optimization/09-03-SUMMARY.md`
</output>
