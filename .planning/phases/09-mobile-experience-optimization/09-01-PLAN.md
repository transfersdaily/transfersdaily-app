---
phase: 09-mobile-experience-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useScrollDirection.ts
  - src/components/navbar.tsx
  - src/components/MobileBottomNav.tsx
autonomous: true
requirements: [MOB-03, MOB-04]

must_haves:
  truths:
    - "Navbar hides when user scrolls down on mobile, reappears when scrolling up"
    - "Bottom nav shows 4 labeled icons (Home, Leagues, Search, Latest) with active state in primary color"
    - "Bottom nav links are locale-aware and highlight the current page"
    - "Collapsing header only activates below md breakpoint (< 768px)"
    - "Desktop navbar remains fixed/sticky at all times"
  artifacts:
    - path: "src/hooks/useScrollDirection.ts"
      provides: "Scroll direction detection hook"
      exports: ["useScrollDirection"]
    - path: "src/components/navbar.tsx"
      provides: "Collapsing header behavior on mobile"
      contains: "useScrollDirection"
    - path: "src/components/MobileBottomNav.tsx"
      provides: "Restyled bottom navigation with locale-aware links"
      contains: "min-h-[48px]"
  key_links:
    - from: "src/components/navbar.tsx"
      to: "src/hooks/useScrollDirection.ts"
      via: "import useScrollDirection"
      pattern: "useScrollDirection"
    - from: "src/components/MobileBottomNav.tsx"
      to: "locale param"
      via: "useParams for locale-aware hrefs"
      pattern: "getLocalizedPath\\|locale"
---

<objective>
Add collapsing header behavior and restyle bottom navigation for mobile.

Purpose: Mobile users get more content area (header hides on scroll down) and native-app-like bottom navigation with locale-aware links and proper active states.
Output: useScrollDirection hook, updated navbar with mobile collapse, restyled MobileBottomNav.
</objective>

<execution_context>
@C:/Users/tarkm/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/tarkm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-mobile-experience-optimization/09-CONTEXT.md

@src/components/navbar.tsx
@src/components/MobileBottomNav.tsx
@src/components/ConditionalLayout.tsx
@src/lib/theme.ts
@src/app/globals.css

<interfaces>
From src/lib/theme.ts:
```typescript
export const zIndex = {
  base: "z-0",
  dropdown: "z-10",
  sticky: "z-20",
  overlay: "z-40",
  modal: "z-[100]",
  toast: "z-[1000]",
} as const

export const motion = {
  fast: "duration-fast",       // 150ms
  normal: "duration-normal",   // 200ms
  slow: "duration-slow",       // 300ms
  respectMotion: "motion-safe:transition-all motion-reduce:transition-none",
} as const
```

From src/components/ConditionalLayout.tsx:
```typescript
// MobileBottomNav is rendered here, main has pb-16 md:pb-0 for bottom nav spacing
export function ConditionalLayout({ children }: { children: React.ReactNode })
```

Existing navbar: sticky top-0 z-20, height h-16. Uses NavLink with min-h-[44px].
Existing MobileBottomNav: fixed bottom-0, z-50, hidden on md:. Has 5 items with hardcoded paths (no locale).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useScrollDirection hook and add collapsing header to navbar</name>
  <files>src/hooks/useScrollDirection.ts, src/components/navbar.tsx</files>
  <read_first>src/components/navbar.tsx, src/app/globals.css</read_first>
  <action>
1. Create `src/hooks/useScrollDirection.ts`:
   - Export `useScrollDirection()` hook returning `"up" | "down"`
   - Use `useState` and `useEffect` with scroll event listener
   - Track `lastScrollY` via `useRef`
   - Threshold: 10px minimum scroll delta before changing direction (per D-13)
   - Cleanup listener on unmount
   - Default to "up" (header visible) on mount

2. Update `src/components/navbar.tsx`:
   - Import `useScrollDirection` from `@/hooks/useScrollDirection`
   - Add state: `const scrollDirection = useScrollDirection()`
   - On the `<header>` element, add conditional classes for mobile collapse:
     - When `scrollDirection === "down"`: add `-translate-y-full` on mobile only
     - When `scrollDirection === "up"`: add `translate-y-0`
     - Use `md:translate-y-0` to ensure desktop NEVER collapses (per D-12)
     - Add `transition-transform duration-[200ms] ease-out` for smooth animation (per D-11)
     - Add `motion-reduce:transition-none` for accessibility
   - The header already has `sticky top-0 z-20` which is correct

The result: on mobile (<768px), scrolling down hides the navbar with a 200ms translateY animation. Scrolling up brings it back. Desktop is unaffected.
  </action>
  <verify>
    <automated>cd C:/Users/tarkm/Desktop/PROJECTS/transfersdaily/transfersdaily-app && npx tsc --noEmit --strict src/hooks/useScrollDirection.ts 2>&1 | head -20; grep -c "useScrollDirection" src/components/navbar.tsx; grep -c "translate-y-full" src/components/navbar.tsx; grep -c "md:translate-y-0" src/components/navbar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep "useScrollDirection" src/hooks/useScrollDirection.ts returns match for export
    - grep "translate-y-full" src/components/navbar.tsx returns match
    - grep "md:translate-y-0" src/components/navbar.tsx returns match
    - grep "duration-\\[200ms\\]\\|duration-normal" src/components/navbar.tsx returns match
    - grep "motion-reduce" src/components/navbar.tsx returns match for transition-none
  </acceptance_criteria>
  <done>Navbar hides on scroll-down on mobile with 200ms ease-out, reappears on scroll-up, desktop unaffected</done>
</task>

<task type="auto">
  <name>Task 2: Restyle MobileBottomNav with locale-aware links and proper design</name>
  <files>src/components/MobileBottomNav.tsx</files>
  <read_first>src/components/MobileBottomNav.tsx, src/components/ConditionalLayout.tsx</read_first>
  <action>
Rewrite `src/components/MobileBottomNav.tsx`:

1. Nav items: 4 items per D-14 (reduce from current 5):
   - Home: `Home` icon from lucide-react, path `/{locale}/`
   - Leagues: `Trophy` icon, path `/{locale}/league/premier-league` (most popular league as default)
   - Search: `Search` icon — onClick opens CommandSearch (import from CommandSearch, manage open state)
   - Latest: `Newspaper` icon, path `/{locale}/latest`

2. Locale awareness:
   - Use `useParams()` to get locale
   - Build paths with `/${locale}/...` pattern
   - For Search, don't navigate — trigger CommandSearch dialog via state

3. Styling per decisions D-15 through D-18:
   - Each item: `min-h-[48px] min-w-[48px]` (48px touch target per MOB-01)
   - Icon + label layout: icon on top, label below (per D-15)
   - Active state: `text-primary` color (per D-16)
   - Inactive: `text-muted-foreground`
   - Container: `fixed bottom-0 left-0 right-0 z-50 bg-background/95 backdrop-blur border-t border-border`
   - Hidden on desktop: `lg:hidden` (per D-18, use lg not md since bottom nav is useful on tablets)
   - Labels: `text-[11px] font-medium mt-0.5`

4. Active detection:
   - Home: active when pathname is exactly `/{locale}` or `/{locale}/`
   - Leagues: active when pathname includes `/league/`
   - Latest: active when pathname includes `/latest`
   - Search: never "active" (it opens a dialog)

5. Import `CommandSearch` and add state management for opening it. Pass locale, open, onOpenChange props.

6. Remove the old Profile/login link — bottom nav is for primary navigation only (per D-17).
  </action>
  <verify>
    <automated>cd C:/Users/tarkm/Desktop/PROJECTS/transfersdaily/transfersdaily-app && grep -c "min-h-\[48px\]" src/components/MobileBottomNav.tsx; grep -c "locale" src/components/MobileBottomNav.tsx; grep -c "text-primary" src/components/MobileBottomNav.tsx; grep -c "lg:hidden" src/components/MobileBottomNav.tsx; grep -c "CommandSearch" src/components/MobileBottomNav.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep "min-h-\[48px\]" src/components/MobileBottomNav.tsx returns at least 1 match
    - grep "locale" src/components/MobileBottomNav.tsx returns matches (locale-aware paths)
    - grep "text-primary" src/components/MobileBottomNav.tsx returns match (active state)
    - grep "lg:hidden" src/components/MobileBottomNav.tsx returns match (hidden on desktop)
    - grep "CommandSearch" src/components/MobileBottomNav.tsx returns match (search integration)
    - No more than 4 nav items in the navItems array
  </acceptance_criteria>
  <done>Bottom nav has 4 locale-aware items with 48px touch targets, primary color active state, search opens CommandSearch dialog, hidden on lg+ screens</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes without errors
- `useScrollDirection` hook exists and is imported by navbar
- Navbar has mobile-only collapse classes with md:translate-y-0 override
- MobileBottomNav has 4 items with 48px targets and locale-aware paths
- Search item in bottom nav triggers CommandSearch dialog
</verification>

<success_criteria>
- Collapsing header works on mobile only with 200ms ease-out transition
- Bottom nav shows Home, Leagues, Search, Latest with icons and labels
- All bottom nav links use locale prefix
- Active nav item highlighted with primary color
- 48px minimum touch targets on bottom nav items
</success_criteria>

<output>
After completion, create `.planning/phases/09-mobile-experience-optimization/09-01-SUMMARY.md`
</output>
