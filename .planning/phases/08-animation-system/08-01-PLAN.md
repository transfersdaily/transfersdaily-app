---
phase: 08-animation-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/components/MotionCard.tsx
  - src/components/PageTransition.tsx
autonomous: true
requirements:
  - DS-06
must_haves:
  truths:
    - "MotionCard wraps children with fade-in-up reveal animation triggered by IntersectionObserver"
    - "PageTransition wraps page content with framer-motion AnimatePresence fade transition"
    - "Both components disable all animation when prefers-reduced-motion is active"
  artifacts:
    - path: "src/components/MotionCard.tsx"
      provides: "Card reveal wrapper with stagger support"
      exports: ["MotionCard"]
    - path: "src/components/PageTransition.tsx"
      provides: "Route-level fade transition wrapper"
      exports: ["PageTransition"]
  key_links:
    - from: "src/components/MotionCard.tsx"
      to: "framer-motion"
      via: "motion.div + useInView"
      pattern: "useInView|motion\\.div"
    - from: "src/components/PageTransition.tsx"
      to: "framer-motion"
      via: "AnimatePresence + motion.div"
      pattern: "AnimatePresence|motion\\.div"
---

<objective>
Create the animation infrastructure: install framer-motion, build MotionCard reveal wrapper and PageTransition fade wrapper. Both components respect prefers-reduced-motion.

Purpose: Provide reusable animation primitives that Plan 02 wires into all public pages.
Output: Two client components (MotionCard, PageTransition) ready for integration.
</objective>

<execution_context>
@C:/Users/tarkm/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/tarkm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-animation-system/08-CONTEXT.md

<interfaces>
<!-- Existing animation tokens from globals.css -->
--duration-fast: 150ms
--duration-normal: 200ms
--duration-slow: 300ms
--easing-default: cubic-bezier(0.4, 0, 0.2, 1)
--easing-out: cubic-bezier(0, 0, 0.2, 1)

<!-- Existing tailwind keyframes already defined: fade-in, fade-in-up, slide-in-right -->
<!-- motion-safe: prefix already used in navbar/footer -->

<!-- ArticleCard props (consumer of MotionCard) from src/components/ArticleCard.tsx -->
export interface ArticleCardProps extends VariantProps<typeof cardVariants> {
  title: string
  href: string
  imageUrl?: string
  league?: string
  timeAgo?: string
  excerpt?: string
  priority?: boolean
  className?: string
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install framer-motion and create MotionCard component</name>
  <files>package.json, src/components/MotionCard.tsx</files>
  <read_first>
    - src/components/ArticleCard.tsx (understand card structure it wraps)
    - src/app/globals.css (animation token values)
    - package.json (current dependencies)
  </read_first>
  <action>
1. Install framer-motion: `npm install framer-motion`

2. Create `src/components/MotionCard.tsx` as a 'use client' component:
   - Props: `children: React.ReactNode`, `index?: number` (for stagger calculation), `className?: string`
   - Use framer-motion `motion.div`, `useInView`, and `useReducedMotion`
   - Animation: opacity 0 to 1, translateY 8px to 0 (matches existing fade-in-up keyframe)
   - Duration: 0.3s (300ms, matches --duration-slow)
   - Easing: [0, 0, 0.2, 1] (matches --easing-out)
   - Stagger: `index * 0.05` seconds delay (50ms per card, per D-05)
   - Use `useInView` with `{ once: true, margin: "-50px" }` so cards animate once when scrolled into view, never re-animate (per D-06)
   - When `useReducedMotion()` returns true, render children without any motion wrapper (just a plain div)
   - Do NOT use `whileInView` -- use `useInView` hook with a ref for better control
   - The component should be a thin wrapper: `<motion.div ref={ref} initial={...} animate={isInView ? ... : ...} transition={...}>{children}</motion.div>`
  </action>
  <acceptance_criteria>
    - grep: "use client" in src/components/MotionCard.tsx
    - grep: "useReducedMotion" in src/components/MotionCard.tsx
    - grep: "useInView" in src/components/MotionCard.tsx
    - grep: "once: true" in src/components/MotionCard.tsx
    - grep: "0.05" in src/components/MotionCard.tsx (stagger delay)
    - grep: "framer-motion" in package.json
    - grep: "export function MotionCard" in src/components/MotionCard.tsx
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/tarkm/Desktop/PROJECTS/transfersdaily/transfersdaily-app && npx tsc --noEmit src/components/MotionCard.tsx 2>&1 | head -20</automated>
  </verify>
  <done>MotionCard component exists, TypeScript compiles, uses useInView with once:true, stagger via index prop, disabled when reduced motion preferred</done>
</task>

<task type="auto">
  <name>Task 2: Create PageTransition component</name>
  <files>src/components/PageTransition.tsx</files>
  <read_first>
    - src/app/[locale]/layout.tsx (where PageTransition will be wired in Plan 02)
    - src/components/MotionCard.tsx (follow same reduced-motion pattern)
  </read_first>
  <action>
1. Create `src/components/PageTransition.tsx` as a 'use client' component:
   - Props: `children: React.ReactNode`, `className?: string`
   - Use framer-motion `motion.div` and `useReducedMotion`
   - Animation: fade-in on mount (opacity 0 to 1)
   - Enter duration: 0.2s (200ms, per D-02)
   - Enter easing: [0, 0, 0.2, 1] (ease-out per D-03)
   - Use `initial={{ opacity: 0 }}`, `animate={{ opacity: 1 }}` pattern
   - Do NOT use AnimatePresence (Next.js App Router does not support exit animations on route change without significant workarounds -- just do entrance fade)
   - When `useReducedMotion()` returns true, render children in a plain div (no motion)
   - Keep the component minimal -- just a motion.div wrapper with the fade-in
   - Export named: `export function PageTransition`
  </action>
  <acceptance_criteria>
    - grep: "use client" in src/components/PageTransition.tsx
    - grep: "useReducedMotion" in src/components/PageTransition.tsx
    - grep: "opacity" in src/components/PageTransition.tsx
    - grep: "0.2" in src/components/PageTransition.tsx (200ms enter duration)
    - grep: "export function PageTransition" in src/components/PageTransition.tsx
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/tarkm/Desktop/PROJECTS/transfersdaily/transfersdaily-app && npx tsc --noEmit src/components/PageTransition.tsx 2>&1 | head -20</automated>
  </verify>
  <done>PageTransition component exists, compiles, fades in on mount at 200ms, disabled when reduced motion preferred</done>
</task>

</tasks>

<verification>
- `npm ls framer-motion` shows framer-motion installed
- Both components TypeScript-compile without errors
- Both components contain `useReducedMotion` for accessibility
- MotionCard uses `useInView` with `once: true` and stagger delay
- PageTransition uses opacity fade-in at 200ms
</verification>

<success_criteria>
framer-motion installed. MotionCard and PageTransition components created, typed, and ready for integration. Both respect prefers-reduced-motion.
</success_criteria>

<output>
After completion, create `.planning/phases/08-animation-system/08-01-SUMMARY.md`
</output>
