---
phase: 08-animation-system
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/components/TransferGrid.tsx
  - src/components/sections/HeroSection.tsx
  - src/components/sections/LatestSection.tsx
  - src/components/sections/LeagueSection.tsx
  - src/app/[locale]/layout.tsx
autonomous: false
requirements:
  - DS-06
must_haves:
  truths:
    - "Cards in TransferGrid reveal with staggered fade-in-up when scrolled into view"
    - "Cards in LeagueSection reveal with staggered fade-in-up when scrolled into view"
    - "HeroSection entrance animates with a fade-in on mount"
    - "Page content fades in on route navigation"
    - "All animations are absent when prefers-reduced-motion is enabled"
  artifacts:
    - path: "src/components/TransferGrid.tsx"
      provides: "MotionCard-wrapped article cards with stagger index"
      contains: "MotionCard"
    - path: "src/components/sections/HeroSection.tsx"
      provides: "MotionCard-wrapped hero section"
      contains: "MotionCard"
    - path: "src/components/sections/LeagueSection.tsx"
      provides: "MotionCard-wrapped league cards with stagger index"
      contains: "MotionCard"
    - path: "src/app/[locale]/layout.tsx"
      provides: "PageTransition wrapping children"
      contains: "PageTransition"
  key_links:
    - from: "src/components/TransferGrid.tsx"
      to: "src/components/MotionCard.tsx"
      via: "import and wrap each card"
      pattern: "import.*MotionCard"
    - from: "src/app/[locale]/layout.tsx"
      to: "src/components/PageTransition.tsx"
      via: "import and wrap children"
      pattern: "import.*PageTransition"
---

<objective>
Wire MotionCard into all card grids (TransferGrid, HeroSection, LeagueSection) and PageTransition into the locale layout. Every public page now has entrance fade and scrolling card reveals.

Purpose: Apply the animation primitives from Plan 01 to all public-facing pages.
Output: All public page cards animate on scroll, pages fade in on navigation.
</objective>

<execution_context>
@C:/Users/tarkm/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/tarkm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-animation-system/08-CONTEXT.md
@.planning/phases/08-animation-system/08-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 - MotionCard -->
export function MotionCard({ children, index, className }: {
  children: React.ReactNode;
  index?: number;
  className?: string;
}): JSX.Element

<!-- From Plan 01 - PageTransition -->
export function PageTransition({ children, className }: {
  children: React.ReactNode;
  className?: string;
}): JSX.Element

<!-- TransferGrid current structure -->
<!-- Wraps each ArticleCard in a <div key={transfer.id} className="col-span-1"> -->
<!-- Grid: grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4 md:gap-6 -->

<!-- LeagueSection current structure -->
<!-- Maps transfers.slice(0,4) into ArticleCard variant="compact" -->
<!-- Grid: grid grid-cols-1 md:grid-cols-2 gap-3 -->

<!-- HeroSection current structure -->
<!-- Grid with hero card + 2 side cards, all ArticleCard variant="hero" -->

<!-- Locale layout wraps: ServerNavbar + AdSlot + {children} inside ConditionalLayout -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire MotionCard into card grids and PageTransition into locale layout</name>
  <files>src/components/TransferGrid.tsx, src/components/sections/HeroSection.tsx, src/components/sections/LeagueSection.tsx, src/components/sections/LatestSection.tsx, src/app/[locale]/layout.tsx</files>
  <read_first>
    - src/components/MotionCard.tsx (Plan 01 output)
    - src/components/PageTransition.tsx (Plan 01 output)
    - src/components/TransferGrid.tsx (wrap each card)
    - src/components/sections/HeroSection.tsx (wrap hero grid)
    - src/components/sections/LeagueSection.tsx (wrap each compact card)
    - src/components/sections/LatestSection.tsx (check if it delegates to TransferGrid)
    - src/app/[locale]/layout.tsx (wrap children)
  </read_first>
  <action>
1. **TransferGrid.tsx** -- Import MotionCard. Wrap each `<div key={transfer.id} className="col-span-1">` content with `<MotionCard index={index}>`. Keep the outer div with key and col-span class as-is; MotionCard goes inside it wrapping the ArticleCard. Pass the forEach `index` as the stagger index.

2. **HeroSection.tsx** -- Import MotionCard. Wrap the entire hero grid container (the `<div className="grid grid-cols-1 md:grid-cols-3 gap-3 h-[420px]">`) in a single `<MotionCard index={0}>` so the whole hero fades in as one unit. Do NOT stagger individual hero cards (they should appear together).

3. **LeagueSection.tsx** -- Import MotionCard. Wrap each ArticleCard inside the `.map()` with `<MotionCard key={transfer.id} index={index}>` where index comes from the map callback. The ArticleCard already has key, so move key to MotionCard and remove it from ArticleCard (or keep both -- React allows key on any element).

4. **LatestSection.tsx** -- This delegates to TransferGrid which already has MotionCard from step 1. No changes needed UNLESS LatestSection renders cards directly. Read file first to confirm.

5. **src/app/[locale]/layout.tsx** -- Import PageTransition. Wrap `{children}` with `<PageTransition>{children}</PageTransition>`. Place it INSIDE ConditionalLayout, wrapping just `{children}` (not the navbar or header ad). The locale layout is a server component, so PageTransition (a client component) can still wrap children because it accepts children as a prop.

Important: Do NOT add 'use client' to any server component file. MotionCard and PageTransition are already client components -- they can be imported and used from server components in Next.js App Router.
  </action>
  <acceptance_criteria>
    - grep: "MotionCard" in src/components/TransferGrid.tsx
    - grep: "MotionCard" in src/components/sections/HeroSection.tsx
    - grep: "MotionCard" in src/components/sections/LeagueSection.tsx
    - grep: "PageTransition" in src/app/[locale]/layout.tsx
    - grep: "import.*MotionCard.*from" in src/components/TransferGrid.tsx
    - grep: "import.*PageTransition.*from" in src/app/[locale]/layout.tsx
    - grep: "index=" in src/components/TransferGrid.tsx (stagger index passed)
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/tarkm/Desktop/PROJECTS/transfersdaily/transfersdaily-app && npx next build 2>&1 | tail -30</automated>
  </verify>
  <done>All card grids use MotionCard for scroll-reveal. Locale layout uses PageTransition for page fade. Build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify animation system visually</name>
  <files>none</files>
  <action>
  Human verifies the complete animation system works correctly across all public pages.
  </action>
  <what-built>Complete animation system: card reveal animations on scroll (staggered fade-in-up), page transition fade on navigation, and reduced-motion support across all public pages.</what-built>
  <how-to-verify>
    1. Start dev server: `npm run dev`
    2. Open http://localhost:3000/en in browser
    3. Verify: Homepage content fades in smoothly on load (PageTransition)
    4. Scroll down past the hero section -- cards in the Latest Transfers grid should animate in with a subtle upward fade, staggered one after another
    5. Continue scrolling to league sections -- compact cards should also reveal with stagger
    6. Navigate to a different page (e.g., click a league link) then go back -- page content should fade in each time
    7. Test reduced motion: In browser DevTools, open Rendering panel, check "Emulate CSS media feature prefers-reduced-motion: reduce" -- reload the page. Verify NO animations occur (content appears instantly)
    8. Check performance: animations should feel smooth (no jank), subtle (not distracting), and fast (under 300ms per card)
  </how-to-verify>
  <verify>Human approval</verify>
  <done>User confirms animations look polished, subtle, and performant. Reduced motion disables all animations.</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `next build` succeeds with no errors
- All card grid files import and use MotionCard
- Locale layout imports and uses PageTransition
- Cards animate on scroll with stagger delay
- Pages fade in on navigation
- No animations when prefers-reduced-motion is enabled
</verification>

<success_criteria>
Public pages feel polished: cards reveal smoothly on scroll, pages fade in on navigation, all animation respects reduced-motion preference. DS-06 requirement satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/08-animation-system/08-02-SUMMARY.md`
</output>
