---
phase: 01-design-tokens-theming
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/lib/typography.ts
  - src/lib/theme.ts
  - tailwind.config.js
  - src/app/globals.css
autonomous: true
requirements: [DS-02, DS-05]
must_haves:
  truths:
    - "Headlines, body text, captions, and metadata render with distinct, intentional sizes and weights across all public pages"
    - "Typography uses serif font for headings and sans-serif for body text, creating an editorial hierarchy"
    - "Spacing, container widths, and grid layouts respond predictably across mobile, tablet, and desktop breakpoints"
    - "Animation tokens define consistent timing and easing for all future motion"
  artifacts:
    - path: "src/lib/typography.ts"
      provides: "Editorial typography scale with font-serif headings and font-sans body"
      contains: "font-serif"
    - path: "src/lib/theme.ts"
      provides: "Spacing scale, animation tokens, z-index scale"
      contains: "zIndex"
    - path: "tailwind.config.js"
      provides: "Custom spacing scale, z-index scale, animation keyframes"
      contains: "--space-"
    - path: "src/app/globals.css"
      provides: "CSS custom properties for spacing scale and editorial typography utilities"
      contains: "--space-1"
  key_links:
    - from: "src/lib/typography.ts"
      to: "tailwind.config.js"
      via: "Typography classes reference font-serif and font-sans which are defined in Tailwind fontFamily (from Plan 01)"
      pattern: "font-serif|font-sans"
    - from: "src/app/globals.css"
      to: "tailwind.config.js"
      via: "Spacing CSS variables consumed by Tailwind spacing config"
      pattern: "--space-"
    - from: "src/lib/theme.ts"
      to: "src/app/globals.css"
      via: "Theme animation/spacing tokens align with CSS custom properties"
      pattern: "duration|easing"
---

<objective>
Build the editorial typography scale and responsive spacing/animation token system on top of the branded colors and fonts established in Plan 01.

Purpose: Gives all subsequent phases (cards, navigation, homepage, articles) a consistent typographic hierarchy and spacing rhythm to build against. The serif/sans font pairing creates the premium editorial feel that distinguishes TransfersDaily from generic blog templates.
Output: Updated typography.ts with editorial scale, theme.ts with spacing/animation/z-index tokens, tailwind.config.js with spacing scale, globals.css with spacing CSS variables and editorial utility classes.
</objective>

<execution_context>
@C:/Users/tarkm/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/tarkm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-design-tokens-theming/01-CONTEXT.md
@.planning/phases/01-design-tokens-theming/01-RESEARCH.md
@.planning/phases/01-design-tokens-theming/01-01-SUMMARY.md
@design-system/transfersdaily/MASTER.md

<interfaces>
<!-- From Plan 01: fontFamily now available in Tailwind -->
<!-- font-serif = Newsreader, font-sans = Roboto -->

From tailwind.config.js (after Plan 01):
```javascript
fontFamily: {
  serif: ['var(--font-serif)', 'Georgia', 'serif'],
  sans: ['var(--font-sans)', 'system-ui', 'sans-serif'],
},
```

From src/lib/typography.ts (current structure to preserve and extend):
```typescript
export const typography = {
  heading: { h1, h2, h3, h4, h5, h6 },
  body: { large, base, small, xs },
  nav: { primary, secondary, dropdown },
  button: { large, default, small },
  badge, caption, label,
  article: { title, subtitle, body, meta },
  card: { title, description, meta },
  logo: { navbar },
}
export const getHeadingClass = (level) => typography.heading[level]
export const getBodyClass = (size) => typography.body[size]
export const getNavClass = (type) => typography.nav[type]
export const getButtonClass = (size) => typography.button[size]
export const responsive = { pageTitle, sectionTitle, cardTitle }
export const mobile = { touchTarget, contentPadding, cardPadding, cardAspect, cardContent, articleContent }
```

From src/lib/theme.ts (current exports to preserve and extend):
```typescript
export function cn(...inputs: ClassValue[]): string
export const transferStatusVariants = { completed, rumor, confirmed, loan, "done-deal", "pending" }
export const leagueVariants = { "premier-league", "la-liga", "bundesliga", "serie-a", "ligue-1" }
export const buttonVariants, cardVariants, textVariants, hoverVariants
export function getTransferStatusClass(status: string): string
export function getLeagueClass(league: string): string
export function getTransferButtonClass(status: string): string
export const modernShadows = { card, elevated, glow, success, warning, error }
export const animations = { fadeIn, fadeInUp, shimmer, delay100, delay200 }
export const spacing = { section, sectionSmall, card, cardSmall }
export const layout = { container, maxWidth, centered, grid2, grid3, grid4 }
export const borders = { modern, accent, success, warning, error }
export const focus = { modern, ring }
export function buildComponentClasses(base, variants, className): string
export const TRANSFER_STATUS_CONFIG
export const LEAGUE_CONFIG
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite typography.ts with editorial serif/sans scale and add CSS utility classes</name>
  <files>src/lib/typography.ts, src/app/globals.css</files>
  <read_first>
    - src/lib/typography.ts (current structure — preserve all export names and helper functions)
    - src/app/globals.css (append editorial utility classes at bottom)
    - .planning/phases/01-design-tokens-theming/01-RESEARCH.md (Typography Scale Update section and Pattern 3: Responsive Typography with clamp())
    - .planning/phases/01-design-tokens-theming/01-CONTEXT.md (Typography decisions section)
  </read_first>
  <action>
**File 1: `src/lib/typography.ts`** — Rewrite with editorial scale.

Preserve ALL existing export names and function signatures. The object structure keys must stay identical so existing imports do not break. Change the CLASS VALUES to use editorial fonts.

Replace the entire `typography` object with:
```typescript
export const typography = {
  // Headings - Editorial Newsreader serif, bold weight hierarchy
  heading: {
    hero: "font-serif text-[clamp(1.75rem,5vw,4rem)] font-black leading-[1.1] tracking-[-0.05em]",
    h1: "font-serif text-2xl md:text-3xl lg:text-4xl font-bold leading-tight tracking-tight",
    h2: "font-serif text-xl md:text-2xl lg:text-3xl font-semibold leading-tight tracking-tight",
    h3: "font-serif text-lg md:text-xl lg:text-2xl font-semibold leading-snug",
    h4: "font-serif text-base md:text-lg font-semibold leading-snug",
    h5: "font-serif text-sm md:text-base font-semibold leading-normal",
    h6: "font-serif text-sm font-semibold leading-normal",
  },

  // Body text - Roboto sans-serif for readability
  body: {
    large: "font-sans text-base md:text-lg leading-relaxed",
    base: "font-sans text-sm md:text-base leading-normal",
    small: "font-sans text-xs md:text-sm leading-normal",
    xs: "font-sans text-xs leading-normal",
  },

  // Navigation and UI elements
  nav: {
    primary: "font-sans text-sm font-medium",
    secondary: "font-sans text-xs font-medium",
    dropdown: "font-sans text-sm",
  },

  // Interactive elements
  button: {
    large: "font-sans text-base font-medium",
    default: "font-sans text-sm font-medium",
    small: "font-sans text-xs font-medium",
  },

  // Special elements
  badge: "font-sans text-xs font-semibold",
  caption: "font-sans text-xs text-muted-foreground",
  label: "font-sans text-sm font-medium",

  // Article-specific - Editorial reading experience
  article: {
    title: "font-serif text-2xl md:text-3xl lg:text-4xl font-bold leading-tight tracking-tight",
    subtitle: "font-sans text-base md:text-lg text-muted-foreground leading-relaxed",
    body: "font-sans text-base md:text-lg leading-relaxed max-w-prose",
    meta: "font-sans text-xs md:text-sm text-muted-foreground",
  },

  // Card-specific
  card: {
    title: "font-serif text-sm md:text-base font-semibold leading-tight",
    description: "font-sans text-xs md:text-sm text-muted-foreground leading-normal",
    meta: "font-sans text-xs text-muted-foreground",
  },

  // Logo sizing
  logo: {
    navbar: "font-serif text-xl md:text-2xl font-bold tracking-tight",
  },
}
```

Note the key changes from current:
- ALL headings now use `font-serif` (Newsreader) — previously no font-family specified (defaulted to Inter)
- ALL body/nav/button/badge/caption/label use `font-sans` (Roboto) explicitly
- New `hero` key added to `heading` with clamp() responsive sizing, font-black (900), tight letter-spacing
- Article title matches h1 scale (was previously smaller)
- Article body uses `text-base md:text-lg` with `max-w-prose` for 65-75ch line length
- Logo uses font-serif with tracking-tight for editorial brand feel
- Card titles use font-serif for editorial consistency

Update the `responsive` object:
```typescript
export const responsive = {
  pageTitle: "font-serif text-xl md:text-2xl lg:text-3xl font-bold tracking-tight",
  sectionTitle: "font-serif text-lg md:text-xl lg:text-2xl font-bold tracking-tight",
  cardTitle: "font-serif text-sm md:text-base font-semibold leading-tight",
}
```

Update the `mobile` object — add `font-serif`/`font-sans` to all class strings:
```typescript
export const mobile = {
  touchTarget: "min-h-[44px] flex items-center",
  contentPadding: "px-4 md:px-6 lg:px-8",
  cardPadding: "p-3 md:p-4",
  cardAspect: "aspect-[16/9] md:aspect-[4/3]",
  cardContent: {
    title: "font-serif text-sm md:text-base font-semibold leading-tight",
    description: "font-sans text-xs md:text-sm text-muted-foreground leading-normal mt-1",
    meta: "font-sans text-xs text-muted-foreground mt-2",
  },
  articleContent: {
    title: "font-serif text-xl md:text-2xl lg:text-3xl font-bold leading-tight mb-3 md:mb-4 tracking-tight",
    body: "font-sans text-base md:text-lg leading-relaxed space-y-3 md:space-y-4",
    meta: "font-sans text-xs md:text-sm text-muted-foreground",
  },
}
```

Keep ALL helper functions exactly as-is: `getHeadingClass`, `getBodyClass`, `getNavClass`, `getButtonClass`.

**File 2: `src/app/globals.css`** — Append editorial typography utility classes.

Add this block AFTER the existing `@layer components` blocks (at the end of the file, after the `mobile-card-grid` class):

```css
/* Editorial typography utilities */
@layer utilities {
  .text-hero {
    font-size: clamp(1.75rem, 5vw, 4rem);
    font-weight: 900;
    letter-spacing: -0.05em;
    line-height: 1.1;
  }

  .text-editorial-body {
    font-size: clamp(1rem, 1.5vw, 1.125rem);
    line-height: 1.75;
    max-width: 65ch;
  }

  .text-section-title {
    font-size: clamp(1.25rem, 3vw, 2rem);
    font-weight: 700;
    letter-spacing: -0.025em;
    line-height: 1.2;
  }
}
```

These CSS utilities complement the Tailwind class strings in typography.ts and provide clamp()-based fluid sizing for use directly in components.
  </action>
  <verify>
    <automated>cd C:/Users/tarkm/Desktop/PROJECTS/transfersdaily/transfersdaily-app && grep "font-serif" src/lib/typography.ts | head -3 && grep "font-sans" src/lib/typography.ts | head -3 && grep "hero" src/lib/typography.ts | head -2 && grep "clamp" src/app/globals.css | head -2 && grep "getHeadingClass" src/lib/typography.ts && npx next build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - typography.ts `heading.hero` contains `font-serif text-[clamp(1.75rem,5vw,4rem)] font-black`
    - typography.ts `heading.h1` contains `font-serif` (not bare text classes)
    - typography.ts `heading.h2` through `heading.h6` all contain `font-serif`
    - typography.ts `body.large` contains `font-sans`
    - typography.ts `body.base` contains `font-sans`
    - typography.ts `article.title` contains `font-serif` and `text-2xl md:text-3xl lg:text-4xl`
    - typography.ts `article.body` contains `font-sans` and `max-w-prose`
    - typography.ts `card.title` contains `font-serif`
    - typography.ts `logo.navbar` contains `font-serif` and `tracking-tight`
    - typography.ts still exports `getHeadingClass`, `getBodyClass`, `getNavClass`, `getButtonClass`
    - typography.ts still exports `responsive` and `mobile` objects
    - globals.css contains `.text-hero` with `clamp(1.75rem, 5vw, 4rem)`
    - globals.css contains `.text-editorial-body` with `max-width: 65ch`
    - globals.css contains `.text-section-title` with `clamp(1.25rem, 3vw, 2rem)`
    - `npx next build` succeeds
  </acceptance_criteria>
  <done>Typography scale rewritten with Newsreader serif headings and Roboto sans body. Hero text uses clamp() for fluid 28-64px sizing. All existing export names and helper functions preserved. Editorial CSS utilities added to globals.css.</done>
</task>

<task type="auto">
  <name>Task 2: Add spacing scale, z-index, animation tokens to theme.ts and tailwind.config.js</name>
  <files>src/lib/theme.ts, tailwind.config.js, src/app/globals.css</files>
  <read_first>
    - src/lib/theme.ts (current exports — extend, do not break)
    - tailwind.config.js (current config — add spacing, zIndex, extend animations)
    - src/app/globals.css (add spacing CSS variables)
    - .planning/phases/01-design-tokens-theming/01-CONTEXT.md (Spacing & Density and Animation Tokens decisions)
    - .planning/phases/01-design-tokens-theming/01-RESEARCH.md (Pattern 4: Spacing Scale as CSS Variables)
  </read_first>
  <action>
**File 1: `src/app/globals.css`** — Add spacing CSS custom properties inside the `:root` selector.

Add these spacing variables AFTER the `--radius: 0.5rem;` line inside `:root`:
```css
    /* Spacing scale (4pt/8dp grid) */
    --space-1: 0.25rem;
    --space-2: 0.5rem;
    --space-3: 0.75rem;
    --space-4: 1rem;
    --space-5: 1.25rem;
    --space-6: 1.5rem;
    --space-8: 2rem;
    --space-10: 2.5rem;
    --space-12: 3rem;
    --space-16: 4rem;
    --space-20: 5rem;
    --space-24: 6rem;

    /* Animation tokens */
    --duration-fast: 150ms;
    --duration-normal: 200ms;
    --duration-slow: 300ms;
    --duration-complex: 400ms;
    --easing-default: cubic-bezier(0.4, 0, 0.2, 1);
    --easing-in: cubic-bezier(0.4, 0, 1, 1);
    --easing-out: cubic-bezier(0, 0, 0.2, 1);
```

These same values do NOT need to be repeated in `.dark` — they are mode-independent.

**File 2: `tailwind.config.js`** — Add spacing, zIndex, and animation extensions.

Inside `theme.extend`, add these new keys (do NOT modify existing colors, fontFamily, borderRadius, or existing keyframes/animation):

Add `spacing` key (AFTER `fontFamily`, BEFORE `borderRadius`):
```javascript
spacing: {
  '1': 'var(--space-1)',
  '2': 'var(--space-2)',
  '3': 'var(--space-3)',
  '4': 'var(--space-4)',
  '5': 'var(--space-5)',
  '6': 'var(--space-6)',
  '8': 'var(--space-8)',
  '10': 'var(--space-10)',
  '12': 'var(--space-12)',
  '16': 'var(--space-16)',
  '20': 'var(--space-20)',
  '24': 'var(--space-24)',
},
```

Add `zIndex` key (AFTER `borderRadius`):
```javascript
zIndex: {
  '0': '0',
  '10': '10',
  '20': '20',
  '40': '40',
  '100': '100',
  '1000': '1000',
},
```

Add `screens` key inside `theme.extend` (AFTER `zIndex`):
```javascript
screens: {
  'xs': '375px',
},
```
Note: Tailwind already has sm:640, md:768, lg:1024, xl:1280, 2xl:1536 built in. We add xs:375 for the smallest mobile breakpoint. The container 2xl:1400px is already configured.

Add to the existing `keyframes` object (merge, do not replace):
```javascript
'fade-in': {
  '0%': { opacity: '0' },
  '100%': { opacity: '1' },
},
'fade-in-up': {
  '0%': { opacity: '0', transform: 'translateY(8px)' },
  '100%': { opacity: '1', transform: 'translateY(0)' },
},
'slide-in-right': {
  '0%': { opacity: '0', transform: 'translateX(16px)' },
  '100%': { opacity: '1', transform: 'translateX(0)' },
},
```

Add to the existing `animation` object (merge, do not replace):
```javascript
'fade-in': 'fade-in var(--duration-normal) var(--easing-out)',
'fade-in-up': 'fade-in-up var(--duration-slow) var(--easing-out)',
'slide-in-right': 'slide-in-right var(--duration-slow) var(--easing-out)',
```

Add `transitionDuration` key (AFTER `animation`):
```javascript
transitionDuration: {
  'fast': 'var(--duration-fast)',
  'normal': 'var(--duration-normal)',
  'slow': 'var(--duration-slow)',
  'complex': 'var(--duration-complex)',
},
transitionTimingFunction: {
  'ease-default': 'var(--easing-default)',
  'ease-in': 'var(--easing-in)',
  'ease-out': 'var(--easing-out)',
},
```

**File 3: `src/lib/theme.ts`** — Add new token exports.

Keep ALL existing exports unchanged. ADD these new exports at the end of the file (before the closing):

```typescript
// Spacing tokens (4pt/8dp grid) — reference for component authors
export const spacingScale = {
  xs: "4px",   // --space-1
  sm: "8px",   // --space-2
  md: "16px",  // --space-4
  lg: "24px",  // --space-6
  xl: "32px",  // --space-8
  "2xl": "48px", // --space-12
  "3xl": "64px", // --space-16
} as const

// Z-index scale — use instead of arbitrary z-values
export const zIndex = {
  base: "z-0",
  dropdown: "z-10",
  sticky: "z-20",
  overlay: "z-40",
  modal: "z-[100]",
  toast: "z-[1000]",
} as const

// Animation token classes — consistent timing across all components
export const motion = {
  // Duration classes
  fast: "duration-fast",       // 150ms
  normal: "duration-normal",   // 200ms
  slow: "duration-slow",       // 300ms
  complex: "duration-complex", // 400ms
  // Easing classes
  easeDefault: "ease-default",
  easeIn: "ease-in",
  easeOut: "ease-out",
  // Preset animations
  fadeIn: "animate-fade-in",
  fadeInUp: "animate-fade-in-up",
  slideInRight: "animate-slide-in-right",
  // Reduced motion helper
  respectMotion: "motion-safe:transition-all motion-reduce:transition-none",
} as const

// Editorial layout helpers — generous whitespace for magazine feel
export const editorial = {
  sectionDesktop: "py-16 lg:py-20",    // Spacious sections on desktop
  sectionMobile: "py-8 md:py-12",       // Tighter on mobile (30-40% reduction)
  articleWidth: "max-w-prose mx-auto",   // 65ch optimal reading width
  heroSpacing: "py-12 md:py-16 lg:py-24", // Extra space around hero content
} as const
```

Do NOT modify any existing exports (cn, transferStatusVariants, leagueVariants, buttonVariants, cardVariants, textVariants, hoverVariants, modernShadows, animations, spacing, layout, borders, focus, buildComponentClasses, TRANSFER_STATUS_CONFIG, LEAGUE_CONFIG, or any functions).
  </action>
  <verify>
    <automated>cd C:/Users/tarkm/Desktop/PROJECTS/transfersdaily/transfersdaily-app && grep "space-1" src/app/globals.css && grep "zIndex" tailwind.config.js && grep "fade-in" tailwind.config.js | head -2 && grep "spacingScale" src/lib/theme.ts && grep "zIndex" src/lib/theme.ts && grep "motion" src/lib/theme.ts | head -2 && grep "editorial" src/lib/theme.ts | head -2 && grep "LEAGUE_CONFIG" src/lib/theme.ts && npx next build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - globals.css `:root` contains `--space-1: 0.25rem` through `--space-24: 6rem` (12 spacing tokens)
    - globals.css `:root` contains `--duration-fast: 150ms` and `--duration-normal: 200ms` and `--duration-slow: 300ms` and `--duration-complex: 400ms`
    - globals.css `:root` contains `--easing-default: cubic-bezier(0.4, 0, 0.2, 1)`
    - tailwind.config.js `spacing` key references `var(--space-1)` through `var(--space-24)`
    - tailwind.config.js contains `zIndex` with keys '0', '10', '20', '40', '100', '1000'
    - tailwind.config.js contains `screens` with `xs: '375px'`
    - tailwind.config.js `keyframes` contains `fade-in`, `fade-in-up`, `slide-in-right`
    - tailwind.config.js `animation` contains `fade-in` referencing `var(--duration-normal)`
    - tailwind.config.js contains `transitionDuration` with `fast`, `normal`, `slow`, `complex`
    - tailwind.config.js contains `transitionTimingFunction` with `ease-default`, `ease-in`, `ease-out`
    - tailwind.config.js still contains existing `accordion-down`, `accordion-up`, `shimmer` keyframes (not deleted)
    - theme.ts exports `spacingScale` with xs through 3xl
    - theme.ts exports `zIndex` with base, dropdown, sticky, overlay, modal, toast
    - theme.ts exports `motion` with fast, normal, slow, fadeIn, fadeInUp, respectMotion
    - theme.ts exports `editorial` with sectionDesktop, sectionMobile, articleWidth, heroSpacing
    - theme.ts still exports `LEAGUE_CONFIG` and `TRANSFER_STATUS_CONFIG` (existing code preserved)
    - theme.ts still exports `cn` function (existing code preserved)
    - `npx next build` succeeds
  </acceptance_criteria>
  <done>4pt/8dp spacing scale defined as CSS variables and Tailwind config. Z-index management scale added. Animation tokens (duration, easing, keyframes) configured. Theme.ts extended with spacingScale, zIndex, motion, and editorial layout helpers. All existing exports preserved.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npx next build` succeeds with no errors
2. `grep -c "font-serif" src/lib/typography.ts` returns 10+ matches (all headings, cards, logo)
3. `grep -c "font-sans" src/lib/typography.ts` returns 15+ matches (all body, nav, button, label)
4. `grep "space-1" src/app/globals.css` confirms spacing scale present
5. `grep "zIndex" tailwind.config.js` confirms z-index scale present
6. `grep "spacingScale" src/lib/theme.ts` confirms new exports present
7. `grep "LEAGUE_CONFIG" src/lib/theme.ts` confirms existing exports preserved
</verification>

<success_criteria>
- Typography.ts uses font-serif for all headings and font-sans for all body text
- Hero text utility with clamp(1.75rem, 5vw, 4rem), font-weight 900, letter-spacing -0.05em
- Article body has max-w-prose for 65-75 character line length
- 12-step spacing scale from 4px to 96px as CSS variables consumed by Tailwind
- Animation tokens: 150ms/200ms/300ms/400ms durations with cubic-bezier easing
- Z-index scale: 0/10/20/40/100/1000
- xs:375px breakpoint added
- All existing exports and functions preserved in both theme.ts and typography.ts
- Build succeeds with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-tokens-theming/01-02-SUMMARY.md`
</output>
